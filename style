/* ============================================================
   UNDIP Maps — style.css
   Design: Clean Utility-Map aesthetic (Navy + Amber accent)
   ============================================================ */

/* ── RESET & ROOT ── */
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

:root {
  --bg:        #f8fafc;
  --surface:   #ffffff;
  --surface2:  #f1f5f9;
  --border:    #e2e8f0;
  --accent:    #fbbf24;
  --accent2:   #1e40af;
  --primary:   #1e3a8a;
  --text:      #0f172a;
  --text-muted:#64748b;
  --sidebar-w: 340px;
  --radius:    12px;
  --shadow:    0 4px 24px rgba(0,0,0,.08);
  --transition:.22s cubic-bezier(.4,0,.2,1);
}

html, body {
  height: 100%;
  font-family: 'Plus Jakarta Sans', sans-serif;
  background: var(--bg);
  color: var(--text);
  overflow: hidden;
}

/* ── SPLASH SCREEN ── */
.splash-screen {
  position: fixed;
  inset: 0;
  z-index: 9999;
  background: linear-gradient(135deg, #1e3a8a 0%, #1e40af 50%, #0f172a 100%);
  display: flex;
  align-items: center;
  justify-content: center;
  animation: fadeOutScreen 0.8s ease-out 2.5s forwards;
}

.splash-content {
  text-align: center;
  animation: slideInContent 0.8s ease-out;
}

.splash-logo {
  font-size: 80px;
  color: var(--accent);
  margin-bottom: 24px;
  animation: bounceIcon 1.2s ease-in-out infinite;
  text-shadow: 0 4px 12px rgba(30, 58, 138, 0.4);
}

.splash-title {
  font-size: 48px;
  font-weight: 800;
  color: #ffffff;
  margin-bottom: 8px;
  font-family: 'Syne', sans-serif;
  letter-spacing: -1px;
  text-shadow: 0 2px 8px rgba(0, 0, 0, 0.3);
}

.splash-subtitle {
  font-size: 18px;
  color: rgba(251, 191, 36, 0.95);
  margin-bottom: 40px;
  font-weight: 400;
}

.splash-loading {
  display: flex;
  justify-content: center;
  gap: 10px;
}

.loading-dot {
  width: 12px;
  height: 12px;
  border-radius: 50%;
  background: var(--accent);
  animation: dotsAnimation 1.4s infinite;
}

.loading-dot:nth-child(2) {
  animation-delay: 0.2s;
}

.loading-dot:nth-child(3) {
  animation-delay: 0.4s;
}

@keyframes slideInContent {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes bounceIcon {
  0%, 100% {
    transform: translateY(0);
  }
  50% {
    transform: translateY(-15px);
  }
}

@keyframes dotsAnimation {
  0%, 60%, 100% {
    opacity: 0.3;
    transform: translateY(0);
  }
  30% {
    opacity: 1;
    transform: translateY(-10px);
  }
}

@keyframes fadeOutScreen {
  from {
    opacity: 1;
    pointer-events: auto;
  }
  to {
    opacity: 0;
    pointer-events: none;
  }
}

/* ── MAP ── */
#map {
  position: fixed;
  inset: 0;
  z-index: 0;
}

/* ── SIDEBAR ── */
.sidebar {
  position: fixed;
  top: 0; left: 0; bottom: 0;
  width: var(--sidebar-w);
  background: linear-gradient(180deg, var(--primary) 0%, #1e40af 100%);
  border-right: 2px solid var(--accent);
  display: flex;
  flex-direction: column;
  z-index: 900;
  box-shadow: var(--shadow);
  transition: transform var(--transition);
  overflow: hidden;
}
.sidebar.collapsed {
  transform: translateX(-100%);
}

/* ── SIDEBAR HEADER ── */
.sidebar-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 18px 16px 14px;
  border-bottom: 2px solid rgba(251, 191, 36, 0.3);
  flex-shrink: 0;
  background: linear-gradient(135deg, #1e3a8a 0%, #1e40af 100%);
}
.logo {
  display: flex;
  align-items: center;
  gap: 12px;
}
.logo-icon {
  width: 42px; height: 42px;
  background: var(--accent);
  border-radius: 10px;
  display: flex; align-items: center; justify-content: center;
  font-size: 20px;
  color: #0f172a;
  flex-shrink: 0;
}
.logo-title {
  display: block;
  font-family: 'Syne', sans-serif;
  font-size: 17px;
  font-weight: 800;
  letter-spacing: .3px;
  color: #ffffff;
}
.logo-sub {
  display: block;
  font-size: 11px;
  color: rgba(251, 191, 36, 0.9);
  letter-spacing: .4px;
}
.sidebar-toggle {
  background: rgba(251, 191, 36, 0.2);
  border: 1px solid rgba(251, 191, 36, 0.5);
  color: #fbbf24;
  border-radius: 8px;
  width: 34px; height: 34px;
  cursor: pointer;
  display: flex; align-items: center; justify-content: center;
  transition: var(--transition);
  flex-shrink: 0;
}
.sidebar-toggle:hover { background: #fbbf24; color: #1e3a8a; border-color: #fbbf24; }

/* ── SIDEBAR OPEN BTN ── */
.sidebar-open-btn {
  position: fixed;
  top: 16px; left: 16px;
  z-index: 950;
  background: var(--surface);
  border: 1px solid var(--border);
  color: var(--accent);
  border-radius: 12px;
  width: 48px; height: 48px;
  cursor: pointer;
  display: flex; align-items: center; justify-content: center;
  font-size: 20px;
  box-shadow: var(--shadow);
  transition: var(--transition);
}
.sidebar-open-btn:hover { background: var(--surface2); }

/* ── SEARCH ── */
.search-wrap {
  position: relative;
  margin: 14px 14px 8px;
  flex-shrink: 0;
}
.search-icon {
  position: absolute;
  left: 12px; top: 50%;
  transform: translateY(-50%);
  color: var(--text-muted);
  font-size: 13px;
  pointer-events: none;
}
#searchInput {
  width: 100%;
  background: var(--surface2);
  border: 1px solid var(--border);
  border-radius: 10px;
  color: var(--text);
  font-family: inherit;
  font-size: 14px;
  padding: 10px 38px 10px 36px;
  outline: none;
  transition: var(--transition);
}
#searchInput::placeholder { color: var(--text-muted); }
#searchInput:focus { border-color: var(--accent); box-shadow: 0 0 0 3px rgba(245,158,11,.15); }
.clear-btn {
  position: absolute;
  right: 10px; top: 50%;
  transform: translateY(-50%);
  background: none; border: none;
  color: var(--text-muted); cursor: pointer;
  display: flex; align-items: center; justify-content: center;
  font-size: 13px;
  padding: 4px;
}
.clear-btn:hover { color: var(--text); }

/* ── DATA SYNC STATUS ── */
.data-sync-status {
  padding: 10px 14px;
  margin: 8px 14px 0;
  background: linear-gradient(135deg, rgba(30, 64, 175, 0.08) 0%, rgba(251, 191, 36, 0.05) 100%);
  border: 1px solid rgba(30, 64, 175, 0.2);
  border-radius: 8px;
  font-size: 11px;
  font-weight: 600;
  color: var(--text);
  display: flex;
  align-items: center;
  gap: 6px;
  flex-shrink: 0;
}

.data-sync-status.disconnected {
  background: rgba(239, 68, 68, 0.08);
  border-color: rgba(239, 68, 68, 0.2);
}

.sync-dot {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  background: #10b981;
  animation: syncPulse 2s ease-in-out infinite;
  flex-shrink: 0;
}

.sync-dot.error {
  background: #ef4444;
  animation: none;
}

@keyframes syncPulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.5; }
}

/* ── FILTER SECTION ── */
.filter-section {
  padding: 0 14px 10px;
  flex-shrink: 0;
}
.section-label {
  font-size: 11px;
  font-weight: 700;
  letter-spacing: .08em;
  text-transform: uppercase;
  color: var(--text-muted);
  margin-bottom: 8px;
  display: flex;
  align-items: center;
  gap: 8px;
}
.count-badge {
  background: var(--accent);
  color: #0f172a;
  font-size: 11px;
  font-weight: 700;
  padding: 1px 7px;
  border-radius: 20px;
  letter-spacing: 0;
  text-transform: none;
}
.filter-chips {
  display: flex;
  flex-wrap: wrap;
  gap: 6px;
}
.chip {
  background: var(--surface2);
  border: 2px solid var(--border);
  color: var(--text-muted);
  border-radius: 20px;
  padding: 6px 14px;
  font-family: inherit;
  font-size: 12px;
  font-weight: 600;
  cursor: pointer;
  display: flex; align-items: center; gap: 6px;
  transition: var(--transition);
  white-space: nowrap;
}
.chip:hover { border-color: #1e40af; color: #1e40af; background: rgba(30, 64, 175, 0.05); }
.chip.active {
  background: linear-gradient(135deg, rgba(30, 64, 175, 0.1) 0%, rgba(251, 191, 36, 0.05) 100%);
  border-color: #1e40af;
  color: #1e40af;
  box-shadow: 0 2px 8px rgba(30, 64, 175, 0.15);
}

/* ── PLACES LIST ── */
.places-section {
  padding: 0 14px 10px;
  flex: 1;
  overflow: hidden;
  display: flex;
  flex-direction: column;
  min-height: 0;
}
.places-list {
  flex: 1;
  overflow-y: auto;
  display: flex;
  flex-direction: column;
  gap: 6px;
  padding-right: 4px;
}
.places-list::-webkit-scrollbar { width: 4px; }
.places-list::-webkit-scrollbar-track { background: transparent; }
.places-list::-webkit-scrollbar-thumb { background: var(--border); border-radius: 4px; }

.place-card {
  display: flex;
  align-items: center;
  gap: 10px;
  background: var(--surface2);
  border: 1px solid var(--border);
  border-radius: 10px;
  padding: 10px 10px 10px 12px;
  cursor: pointer;
  transition: var(--transition);
  flex-shrink: 0;
}
.place-card:hover { border-color: var(--accent); background: rgba(251, 191, 36, 0.08); transform: translateX(2px); }
.place-card.active { border-color: var(--accent); background: rgba(245,158,11,.08); }

.place-icon {
  width: 38px; height: 38px;
  border-radius: 9px;
  display: flex; align-items: center; justify-content: center;
  font-size: 15px;
  flex-shrink: 0;
}
.place-info {
  flex: 1;
  min-width: 0;
}
.place-name {
  display: block;
  font-size: 13px;
  font-weight: 700;
  color: var(--text);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}
.place-cat {
  display: block;
  font-size: 11px;
  color: var(--text-muted);
  margin-top: 2px;
}
.place-nav-btn {
  background: var(--surface);
  border: 1px solid var(--border);
  color: var(--accent2);
  border-radius: 8px;
  width: 32px; height: 32px;
  cursor: pointer;
  display: flex; align-items: center; justify-content: center;
  font-size: 14px;
  transition: var(--transition);
  flex-shrink: 0;
}
.place-nav-btn:hover { background: var(--accent2); color: white; border-color: var(--accent2); }

.empty-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 8px;
  padding: 40px 0;
  color: var(--text-muted);
  font-size: 14px;
}
.empty-state i { font-size: 28px; opacity: .4; }

/* ── ROUTE PANEL ── */
.route-panel {
  border-top: 1px solid var(--border);
  background: var(--surface2);
  flex-direction: column;
  flex-shrink: 0;
  max-height: 280px;
}
.route-header {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 12px 14px;
  font-size: 13px;
  font-weight: 700;
  color: var(--accent);
  border-bottom: 1px solid var(--border);
}
.route-header span { flex: 1; }
.close-route {
  background: none; border: none;
  color: var(--text-muted); cursor: pointer;
  padding: 4px; font-size: 14px;
  transition: var(--transition);
  display: flex;
}
.close-route:hover { color: var(--text); }

.route-info {
  padding: 10px 14px 14px;
  overflow-y: auto;
  flex: 1;
}
.route-info::-webkit-scrollbar { width: 4px; }
.route-info::-webkit-scrollbar-thumb { background: var(--border); }

.route-dest {
  font-size: 13px;
  font-weight: 700;
  color: var(--text);
  margin-bottom: 8px;
  display: flex; align-items: center; gap: 6px;
}
.route-dest i { color: #ef4444; }
.route-stats {
  display: flex; gap: 16px;
  margin-bottom: 10px;
}
.route-stats span {
  font-size: 13px;
  color: var(--text-muted);
  display: flex; align-items: center; gap: 5px;
}
.route-stats i { color: var(--accent); }
.route-steps {
  list-style: none;
  display: flex;
  flex-direction: column;
  gap: 6px;
}
.route-steps li {
  font-size: 12px;
  color: var(--text-muted);
  display: flex; align-items: flex-start; gap: 6px;
  line-height: 1.5;
}
.step-dot {
  color: var(--accent2);
  font-size: 9px;
  margin-top: 4px;
  flex-shrink: 0;
}

/* ── FAB ── */
.fab {
  position: fixed;
  bottom: 100px; right: 16px;
  z-index: 900;
  background: var(--surface);
  border: 1px solid var(--border);
  color: var(--accent2);
  border-radius: 50%;
  width: 50px; height: 50px;
  cursor: pointer;
  display: flex; align-items: center; justify-content: center;
  font-size: 18px;
  box-shadow: var(--shadow);
  transition: var(--transition);
}
.fab:hover { background: var(--accent2); color: white; border-color: var(--accent2); transform: scale(1.08); }
.fab.active { background: var(--accent2); color: white; border-color: var(--accent2); }
.fab.active i { animation: spin-once .4s ease; }
@keyframes spin-once {
  from { transform: rotate(-90deg) scale(.7); opacity: 0; }
  to   { transform: rotate(0deg) scale(1);   opacity: 1; }
}

/* ── TOAST ── */
.toast {
  position: fixed;
  bottom: 30px; left: 50%;
  transform: translateX(-50%) translateY(80px);
  background: var(--surface);
  border: 1px solid var(--border);
  color: var(--text);
  padding: 10px 18px;
  border-radius: 40px;
  font-size: 13px;
  font-weight: 600;
  display: flex; align-items: center; gap: 8px;
  box-shadow: var(--shadow);
  z-index: 9999;
  opacity: 0;
  transition: all .3s cubic-bezier(.4,0,.2,1);
  white-space: nowrap;
}
.toast.show { opacity: 1; transform: translateX(-50%) translateY(0); }
.toast.success { border-color: #10b981; }
.toast.success i { color: #10b981; }
.toast.error { border-color: #ef4444; }
.toast.error i { color: #ef4444; }
.toast.warning { border-color: var(--accent); }
.toast.warning i { color: var(--accent); }
.toast.info i { color: var(--accent2); }

/* ── LEAFLET OVERRIDES ── */
.leaflet-control-zoom {
  border: none !important;
  margin-bottom: 10px !important;
}
.leaflet-control-zoom a {
  background: var(--surface) !important;
  border: 1px solid var(--border) !important;
  color: var(--text) !important;
  border-radius: 8px !important;
  margin-bottom: 4px !important;
  font-size: 16px !important;
  line-height: 30px !important;
  width: 32px !important; height: 32px !important;
  transition: var(--transition) !important;
}
.leaflet-control-zoom a:hover { background: var(--surface2) !important; border-color: var(--accent) !important; }
.leaflet-control-scale-line {
  background: rgba(15,23,42,.75) !important;
  color: var(--text) !important;
  border-color: var(--border) !important;
  font-size: 11px !important;
}
.leaflet-routing-container { display: none !important; }
.leaflet-tile-pane { filter: brightness(.85) saturate(.7) hue-rotate(180deg); }

/* ── CUSTOM MARKER ── */
.custom-marker {
  width: 38px; height: 38px;
  background: var(--mc, #3b82f6);
  border-radius: 50% 50% 50% 0;
  transform: rotate(-45deg);
  display: flex; align-items: center; justify-content: center;
  border: 3px solid #fff;
  box-shadow: 0 3px 12px rgba(0,0,0,.4);
  transition: transform .15s;
}
.custom-marker:hover { transform: rotate(-45deg) scale(1.12); }
.custom-marker i {
  transform: rotate(45deg);
  font-size: 14px;
  color: white;
}

/* ── USER MARKER ── */
.user-marker {
  position: relative;
  width: 24px; height: 24px;
}
.user-marker .dot {
  position: absolute;
  top: 50%; left: 50%;
  transform: translate(-50%,-50%);
  width: 14px; height: 14px;
  background: #2563eb;
  border: 3px solid white;
  border-radius: 50%;
  box-shadow: 0 2px 8px rgba(37,99,235,.6);
  z-index: 2;
}
.user-marker .pulse {
  position: absolute;
  top: 50%; left: 50%;
  transform: translate(-50%,-50%);
  width: 40px; height: 40px;
  background: rgba(37,99,235,.2);
  border-radius: 50%;
  animation: pulse 1.8s ease-out infinite;
}
@keyframes pulse {
  0%   { transform: translate(-50%,-50%) scale(.3); opacity: 1; }
  100% { transform: translate(-50%,-50%) scale(1.6); opacity: 0; }
}

/* ── POPUP ── */
.leaflet-popup-content-wrapper.custom-popup,
.custom-popup .leaflet-popup-content-wrapper {
  background: var(--surface) !important;
  border: 1px solid var(--border) !important;
  border-radius: var(--radius) !important;
  box-shadow: var(--shadow) !important;
  padding: 0 !important;
  overflow: hidden;
}
.custom-popup .leaflet-popup-tip {
  background: var(--surface) !important;
}
.popup-card { padding: 14px; min-width: 220px; }
.popup-cat {
  display: inline-flex; align-items: center; gap: 5px;
  font-size: 11px; font-weight: 700;
  border-radius: 6px;
  padding: 3px 9px;
  margin-bottom: 8px;
  letter-spacing: .04em;
  text-transform: uppercase;
}
.popup-name {
  font-family: 'Syne', sans-serif;
  font-size: 15px;
  font-weight: 800;
  color: var(--text);
  margin-bottom: 5px;
  line-height: 1.3;
}
.popup-addr {
  font-size: 12px;
  color: var(--text-muted);
  display: flex; align-items: center; gap: 5px;
  margin-bottom: 6px;
}
.popup-addr i { color: #ef4444; flex-shrink: 0; }
.popup-desc {
  font-size: 12px;
  color: var(--text-muted);
  margin-bottom: 12px;
  line-height: 1.5;
}
.popup-route-btn {
  width: 100%;
  background: var(--accent2);
  border: none;
  color: white;
  border-radius: 8px;
  padding: 9px;
  font-family: inherit;
  font-size: 13px;
  font-weight: 700;
  cursor: pointer;
  display: flex; align-items: center; justify-content: center; gap: 7px;
  transition: var(--transition);
}
.popup-route-btn:hover { background: #1d4ed8; }

/* ── RESPONSIVE ── */
@media (max-width: 640px) {
  :root { --sidebar-w: 300px; }
  .filter-chips { gap: 4px; }
  .chip { padding: 4px 9px; font-size: 11px; }
}